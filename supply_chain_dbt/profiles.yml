supply_chain_dbt:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: supply_chain
      schema: 'main'
      schemas_and_paths:
        main: "{{ env_var('DBT_DB_PATH', '../db/supply_chain.db') }}"
      schema_directory: "{{ env_var('DBT_SCHEMA_DIR', '../db') }}"
  target: dev